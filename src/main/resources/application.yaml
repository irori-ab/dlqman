quarkus:
  http:
    port: 8888
  #log:
  #  level: DEBUG
  kafka:
    devservices:
      port: 9992
  hibernate-orm:
    database:
      generation: drop-and-create
    #log:
    #  sql: true
  datasource:
    db-kind: postgresql
source:
  kafka:
    - bootstrap-servers: localhost:9992
      group-id: group-id
      topics:
        - spring-cloud-dlq-topic